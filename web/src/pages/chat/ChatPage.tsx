import Page from "../../components/Page";

const ChatPage = () => {
    return (
        <Page className="items-center justify-center" requiresAuth={true}>
            <div></div>
        </Page>
    );
};

export default ChatPage;
